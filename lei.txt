#include<stdio.h.>
int main()
{
	struct Save//´¢´æ½á¹û
	{
		char b[100][100];
		int N;
		int M;
		int A;
	}save[100];
	int n,m,i,x,j,k;
	char a[100][100];
	for(x=0;;x++)
	{
		scanf("%d %d",&n,&m);
		if(n==0||m==0)
			break;
		else
		{
			for(i=0;i<n;i++)
				scanf("%s",a[i]);
			for(i=0;i<n;i++)
				for(j=0;j<m;j++)
					if(a[i][j]=='.')
						a[i][j]='0';
			for(i=0;i<n;i++)
				for(j=0;j<m;j++)
					if(a[i][j]=='0')
					{
						if(a[i-1][j-1]=='*')
							a[i][j]++;
						if(a[i-1][j]=='*')
							a[i][j]++;
						if(a[i-1][j+1]=='*')
							a[i][j]++;
						if(a[i][j-1]=='*')
							a[i][j]++;
						if(a[i][j+1]=='*')
							a[i][j]++;
						if(a[i+1][j-1]=='*')
							a[i][j]++;
						if(a[i+1][j]=='*')
							a[i][j]++;
						if(a[i+1][j+1]=='*')
							a[i][j]++;
					}
			for(i=0;i<n;i++)
				for(j=0;j<m;j++)
					save[x].b[i][j]=a[i][j];
			save[x].N=n,save[x].M=m;
		}
	}
	for(k=0;k<x;k++)
	{
		for(i=0;i<save[k].N;i++)
		{
			for(j=0;j<save[k].M;j++)
				printf("%c",save[k].b[i][j]);
			printf("\n");
		}
		printf("\n");
	}
	return 0;
}